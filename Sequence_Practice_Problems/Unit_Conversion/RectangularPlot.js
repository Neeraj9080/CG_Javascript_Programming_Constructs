let lengthInFeet = 30;
let widthInFeet = 20;
let feetToMeterConversionFactor = 0.3048;

let lengthInMeters = lengthInFeet * feetToMeterConversionFactor;
let widthInMeters = widthInFeet * feetToMeterConversionFactor;

console.log("Length in meters : ", lengthInMeters);
console.log("Width in meters : ", widthInMeters);
